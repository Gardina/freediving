import React from 'react';

import Navigation from 'Navigation';

const Header = (props) => {
	return (
      <div>
        <h1>Evolution freediving</h1>
				<Navigation/>
      </div>
	);
};

export default Header;
